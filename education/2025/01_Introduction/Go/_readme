Ce programme Go récupère et affiche les derniers titres d'actualité liés à l'IA depuis le service NewsAPI.org. Il crée une URL avec une requête de recherche pour « IA » et une clé API, puis utilise le package net/http pour envoyer une requête HTTP GET au point de terminaison NewsAPI. Le corps de la réponse brute, au format JSON, est lu et démarshalisé en structures Go (NewsResponse et Article) qui décrivent la structure de la réponse JSON de l'API.

Une fois les données décodées, le programme parcourt les 5 principaux articles renvoyés et affiche le titre de chaque article, le nom de la source et un lien vers l'article complet. En résumé, ce code montre comment utiliser une API REST en Go, analyser sa réponse JSON et afficher des résultats lisibles par l'utilisateur sur le terminal.
